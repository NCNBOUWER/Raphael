# FINAL_PRE_PUBLISH_RELEASE_NOTES_v1_5

## Summary

This pass finalizes the clean publish-candidate structure for the Trinity Probability Well / Raphael GeoMatrices side-channel proof package.

## Major corrections locked

1. `(7,7,7)` is the unique product-probability mode.
2. Barycentric entropy/centroid selection is non-unique across equal triples.
3. `(7,7,7)` is therefore the unique probability-selected member of the centroid/equal-triple set.
4. `(7,...,7)` is the finite N-variable product-measure mode for independent 2d6 sum variables.
5. `Γ` is a continuous trajectory bridge over simplex states, not a proof of physical field equations.
6. TPW supports, constrains, classifies, and can falsify correspondence claims; it does not rewrite the Raphael Equations.

## Validation

The code harness compiles and its tests pass. The validation layer checks product probability, normalization, barycentric closure, convex projection, and the Γ trajectory bridge.

## Remaining publication dependencies

- Human sign-off on citation page/section mapping.
- Human sign-off that Raphael equation references are cross-referenced without modification.
- Human decision on whether audit-pending physical correspondence claims are excluded or retained as P5 hypotheses.
