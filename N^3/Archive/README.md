# Archive

This folder is reserved for deliberately retained archive notes. Earlier versioned artefacts should not remain in the clean public-facing main tree unless intentionally placed here.

Current policy: remove old build artefacts from main where possible and rely on Git history plus Drive backup for recovery.
