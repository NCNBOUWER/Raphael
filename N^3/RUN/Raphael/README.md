# Raphael interface housing

This folder is the standalone placeholder for future integration with the broader private Raphael Git repository.

Current rule:

- This repository does not publish or rewrite the private Raphael Equations.
- Files here define adapter boundaries, call surfaces, and future integration points only.
- Updates here should not alter the standalone TPW proof package unless explicitly reviewed.

Use `adapter_manifest.json` as the controlled list of future calls/imports.
